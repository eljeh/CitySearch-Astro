---
// Import the Card component
import Card from './Card.astro';

// fetch data from ./data/apartmentList.json
const { apartments } = await fetch('../data/apartmentList.json').then(res => res.json())

---
// loop through the apartments array
{apartments.map(apartment => (
  <Card {...apartment} />
))}
<Card />
